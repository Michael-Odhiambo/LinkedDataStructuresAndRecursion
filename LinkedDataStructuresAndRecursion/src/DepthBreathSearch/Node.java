package DepthBreathSearch;

/**
 * Represents a node in a linked list of locations. Both the Stack and the Queue class use this type of
 * linked list.
 */
class Node {

    Location loc;  // Represents one square in the grid.
    Node next;  // Pointer to the next Node in the linked list.

}
